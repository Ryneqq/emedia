let original = [];
let fttRe = [];
let fftIm = [];
let encrypted = [];

class Edit {
    constructor() {
        arrayCopy(pixels, original);
        arrayCopy(pixels, fftRe);
        arrayCopy(pixels, fftIm);
        arrayCopy(pixels, encrypted);
    }
}